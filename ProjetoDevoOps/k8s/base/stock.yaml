apiVersion: apps/v1
kind: Deployment
metadata:
  name: stock
  namespace: loja-veloz
spec:
  replicas: 2
  selector:
    matchLabels:
      app: stock
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        app: stock
    spec:
      securityContext:
        runAsNonRoot: true
      containers:
      - name: stock
        image: ghcr.io/exemplo/loja-veloz-stock:1.0.0
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8003
        envFrom:
        - configMapRef:
            name: loja-veloz-config
        readinessProbe:
          httpGet:
            path: /ready
            port: 8003
          initialDelaySeconds: 5
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /health
            port: 8003
          initialDelaySeconds: 10
          periodSeconds: 15
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 256Mi
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: false
          capabilities:
            drop: ["ALL"]
---
apiVersion: v1
kind: Service
metadata:
  name: stock
  namespace: loja-veloz
spec:
  selector:
    app: stock
  ports:
  - port: 8003
    targetPort: 8003
